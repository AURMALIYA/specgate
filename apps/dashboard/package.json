{
  "name": "@specgate/dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Static SpecGate dashboard served by @specgate/api (registry, conflicts, tier distribution, metrics).",
  "scripts": {
    "build": "node -e \"console.log('dashboard is static; served by @specgate/api from public/')\""
  }
}
